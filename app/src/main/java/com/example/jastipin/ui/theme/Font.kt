package com.example.jastipin.ui.theme

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import com.example.jastipin.R

// Inter Font
var interRegular = FontFamily(Font(resId = R.font.interregular))
var interMedium = FontFamily(Font(resId = R.font.intermedium))
var interBlack = FontFamily(Font(resId = R.font.interblack))
var interSemiBold = FontFamily(Font(resId = R.font.intersemibold))
var interBold = FontFamily(Font(resId = R.font.interbold))
var interExtraBold = FontFamily(Font(resId = R.font.interextrabold))

// Nunito Font
var nunitoSemiBold = FontFamily(Font(resId = R.font.nunitosemibold))
var nunitoBold = FontFamily(Font(resId = R.font.nunitobold))
var nunitoRegular = FontFamily(Font(resId = R.font.nunitoregular))
var nunitoMedium = FontFamily(Font(resId = R.font.nunitomedium))